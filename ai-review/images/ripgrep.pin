# OpenCode's grep/glob tools shell out to ripgrep. Its resolution order is
# which("rg") -> $HOME/.cache/opencode/bin/rg -> download from GitHub releases.
# The download verifies nothing but a non-zero byte length, and the adapter gives
# each run a fresh HOME, so the cache is always cold and every review would fetch
# and exec an unverified binary. Installing this pinned, checksum-verified copy on
# PATH short-circuits at the first step.
#
# Keep the version equal to the one the pinned opencode-ai would fetch, so the
# reviewer runs the ripgrep OpenCode was tested against. The version here and the
# port of the artifact URL are bound to opencode-ai's downloads; refresh steps,
# including how to observe the version opencode would fetch, live in
# ai-review/images/SUPPLY_CHAIN.md.
#
# opencode_version must equal the opencode-ai version in
# ai-review/images/package.json, and check_supply_chain_pins.py enforces that
# equality. It is what makes a future opencode bump fail the pin check until the
# ripgrep version it fetches has been re-observed, instead of silently stranding
# the reviewer on a mismatched ripgrep.
#
# Two digests, because they cover different things: `sha256` is the release
# tarball, verified at download time in the ripgrep-bin builder stage;
# `binary_sha256` is the extracted `rg` that lands on /usr/local/bin/rg, asserted
# again in the final image stage and at preflight, so what actually resolves on
# the adapter's PATH is checked rather than merely what was downloaded.
version=15.1.0
opencode_version=1.18.12
url=https://github.com/BurntSushi/ripgrep/releases/download/15.1.0/ripgrep-15.1.0-x86_64-unknown-linux-musl.tar.gz
sha256=1c9297be4a084eea7ecaedf93eb03d058d6faae29bbc57ecdaf5063921491599
binary_sha256=ebeaf56f8a25e102e9419933423738b3a2a613a444fd749d695e15eba53f71f2
